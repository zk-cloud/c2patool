name: Build arm64 c2patool
on: [workflow_dispatch] # 允许手动触发
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4 # <-- 这里升级到了 v4
      
      - name: Install Rust
        uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          target: aarch64-unknown-linux-gnu
          override: true
          
      - name: Install cross
        run: cargo install cross
        
      - name: Build with cross
        run: cross build --target aarch64-unknown-linux-gnu --release
        
      - name: Upload Artifact
        uses: actions/upload-artifact@v4 # <-- 这里升级到了 v4
        with:
          name: c2patool-linux-arm64
          path: target/aarch64-unknown-linux-gnu/release/c2patool
